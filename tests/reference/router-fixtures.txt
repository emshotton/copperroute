# Router reference stems for crates/copper-router/tests/{reference_parity,batch_parity}.rs.
#
#   stem|dsn|max_items|ripup_pass_no[|max_passes|fanout|optimizer]
#
# `dsn` is relative to tests/corpus. `max_items` is how many connections reference_parity routes
# one by one (a number larger than the board's count means every connection); `ripup_pass_no`
# is the pass multiplier applied to the ripup costs, `-` meaning the default 1. A `-` in
# `max_items` marks a row batch_parity alone uses. `max_passes|fanout|optimizer` are the
# whole-board columns: the pass cap and the two pipeline stages; a row without them is
# per-connection only.
#
# router-rpi-splitter, router-j2-reference, router-ecc83-input and router-empty-board run in CI;
# the others are `#[cfg_attr(debug_assertions, ignore)]` and run under COPPERROUTE_SLOW=1 --release.
router-rpi-splitter|fixtures/Issue143-rpi_splitter.dsn|8|-|8|on|on
router-dac2020-bm01|fixtures/Issue508-DAC2020_bm01.dsn|100000|-|2|on|on
router-j2-reference|fixtures/Issue026-J2_reference.dsn|100000|-|99|on|on
router-tutorial-board|examples/tutorial_board/tutorial_board.dsn|8|-|8|on|on
router-ecc83-input|fixtures/Issue649-kicad_ecc83-pp_input_board_v1.dsn|100000|-|8|on|on
router-dac2020-bm01-pass2|fixtures/Issue508-DAC2020_bm01.dsn|100000|2
router-fanout-bm11|fixtures/Issue730-DAC2020_bm11.dsn|-|-|2|on|off
router-strict-drc-cnh|fixtures/Issue555-CNH_Functional_Tester_1.dsn|-|-|2|on|on
router-empty-board|fixtures/empty_board.dsn|-|-|1|off|off
