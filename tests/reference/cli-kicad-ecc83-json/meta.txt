generated by port (target/release/freerouting)
port sha     7b5fac1651b4
plan 9 task  T9
java         n/a (no JVM runs in this lane)
hash mode    n/a (the port has no Object.hashCode)
lane         ci
budget       fr_core::RouterBudget::default() — what a user gets. Since Plan 9
             Task 1 (#234) that is opt_changed_area_ms = 0, Java's own off
             value (TraceTightener.java:73-77), so the pull-tight runs to
             completion and these bytes do not depend on how fast this host
             is. The other three keep Java's literals: fanout_ms_per_pin
             10000, board_update_throttle_ms 250, progress_throttle_ms 1000.
             Nothing is disabled — RouterBudget::disabled() is the parity
             drivers' configuration and is stricter than this one.
command      freerouting -de <FREEROUTING_JAVA_DIR>/fixtures/Issue649-kicad_ecc83-pp_input_board_v1.json -do <OUT>/route.ses -mp 3 --router.fanout.enabled=true --router.optimizer.enabled=true
manifest cmd freerouting -de <FREEROUTING_JAVA_DIR>/fixtures/Issue649-kicad_ecc83-pp_input_board_v1.json -do <OUT>/route.ses -mp 3 --router.fanout.enabled=true --router.optimizer.enabled=true --router.result_json=<OUT>/manifest.json
exit code    0
ses bytes    3841
ses sha256   c37e9f5e70c5758de0b4a170ad1463de2c4ddf18064bf0b01b91cff26b8f6c1e
batch cross-check  n/a (no tests/reference/kicad-ecc83-json/batch.ses)
