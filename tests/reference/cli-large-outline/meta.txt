generated by port (target/release/freerouting)
port sha     46e4a5704388
plan 9 task  via-and-trace-length
java         n/a (no JVM runs in this lane)
hash mode    n/a (the port has no Object.hashCode)
lane         slow
budget       fr_core::RouterBudget::default() — what a user gets. Since Plan 9
             Task 1 (#234) that is opt_changed_area_ms = 0, Java's own off
             value (TraceTightener.java:73-77), so the pull-tight runs to
             completion and these bytes do not depend on how fast this host
             is. The other three keep Java's literals: fanout_ms_per_pin
             10000, board_update_throttle_ms 250, progress_throttle_ms 1000.
             Nothing is disabled — RouterBudget::disabled() is the parity
             drivers' configuration and is stricter than this one.
command      freerouting -de <FREEROUTING_JAVA_DIR>/fixtures/Issue035-ReadPlaceScope.dsn -do <OUT>/route.ses -mp 2 --router.fanout.enabled=true --router.optimizer.enabled=false
manifest cmd freerouting -de <FREEROUTING_JAVA_DIR>/fixtures/Issue035-ReadPlaceScope.dsn -do <OUT>/route.ses -mp 2 --router.fanout.enabled=true --router.optimizer.enabled=false --router.result_json=<OUT>/manifest.json
exit code    0
ses bytes    96701
ses sha256   5413db0619235c87bfddfae40d870b7dbbd1aa743e1fb2fb52b0c1cdf5b2e110
batch cross-check  n/a (no tests/reference/large-outline/batch.ses)
