xdiff        OPEN — the port matches this transcript for connections 1-174 and diverges at
             connection 175 of 294 (Java `maxIdAfter` 292240, the port 292230). The whole board
             — every trace and via corner list, every item's nets, clearance class and fixed
             state, and the changed area — is byte-identical on both sides through connection
             174 (`P6T1_DUMP_BOARD=1`), Java is hash-mode independent here (`P5T_HASH_MODE=0`
             == `=2`), `p7t3` modes 0-4 MATCH on this board at routeK 60/120/180, and the same
             board at `ripupPassNo = 2` MATCHes end to end. Localised to the `Line`
             reference-identity model (quirks #74/#188): forcing `Line::is_same_object` to
             `false` moves the first divergence to connection 83. Controller ruling AX opened
             Task 8b to bisect it. `crates/fr-router/tests/reference_parity.rs`'s
             `steps_one_to_eight_on_dac2020_is_the_open_xdiff_at_connection_175` pins both
             halves — 1-174 identical, 175 different — so the row cannot silently widen or
             silently close. See
             `.superpowers/sdd/2026-08-30-plan-7-router-batch/task-8-report.md` §7.
