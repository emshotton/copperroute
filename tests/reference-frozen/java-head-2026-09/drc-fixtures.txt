# DRC reference fixtures for scripts/gen-drc-reference.sh (Plan 5 Task 9).
#
#   stem|dsn|rules|ses
#
# All three path columns are relative to the Java checkout ($FREEROUTING_JAVA_DIR, default
# ../freerouting). `rules` and `ses` are optional; when present the generator passes them to the
# HEAD jar's `-drc` mode exactly as `Freerouting.initializeDrc` consumes them — the rules file
# through `-dr` (GlobalSettings.java:670-674) and the session file through the `-de` slot list
# (GlobalSettings.java:564-648), which is the *only* way to reach
# `globalSettings.designSessionFilename`: there is no `-ds` flag anywhere in the parser.
#
# The stems are prefixed `drc-` so they cannot collide with the DSN/SES reference stems in
# fixtures.txt, which are generated by a different script from a different jar.
drc-dev-board|fixtures/Issue575-drc_dev-board_4_hole_clearance_violations.dsn||
drc-bbd-mars-64|fixtures/Issue575-drc_BBD_Mars-64_6_track_1_hole_clearance_violations.dsn||
drc-natural-tone-preamp|fixtures/Issue575-drc_Natural_Tone_Preamp_7_unconnected_items.dsn||
drc-issue593-rules|fixtures/Issue593-BBD_Mars-64.dsn|fixtures/Issue593-BBD_Mars-64.rules|
drc-issue593-ses|fixtures/Issue593-BBD_Mars-64.dsn||fixtures/Issue593-BBD_Mars-64.ses
drc-issue753-cpu85|fixtures/Issue753-CPU-85_r104.dsn||
drc-issue110-relay|fixtures/Issue110-RelayModule.dsn||
drc-tutorial-board|examples/tutorial_board/tutorial_board.dsn||
