Plan 9 Task 8 — CURRENT port behaviour, captured from the repo at tag v1.0.0 (ecc0abf),
cloned read-only to $SCRATCH/port-v100 and built there. The working tree at
plan-9-post-parity was never touched.

================================================================================
#159 — the divide_large_room arithmetic behind the '4'
================================================================================
=== #18 IntBox::divide_into_sections ===
  flat box dim-1 divide(30) -> 0 sections: []
  20000-square divide(10000) -> 4 sections

  (so a 20000-unit square divided at max_section_width 10000 gives exactly 4 sections)

Call sites of divide_large_room in crates/fr-router/src/autoroute/tree_ext.rs:
  :491  complete_shape_base   -> calls it
  :835  complete_shape_45     -> calls it
  :1261 complete_shape_90     -> '// :190. This regime never calls divideLargeRoom.'
  => the fix as sketched gives [4, 4, 1]; [4, 4, 4] needs the division added too.

================================================================================
#159 — the 90-degree fixture actually drives the regime
================================================================================
$ ./target/release/freerouting -de p9t8-ninety-degree.dsn -do p9t8.ses   (exit 0, no stderr)
Routed session (both nets complete, every segment axis-aligned):
  (routes 
    (resolution um 10)
    (parser
    )
    (library_out 
      (padstack VIA1
        (shape
          (circle F.Cu 60000 0 0)
        )
        (shape
          (circle B.Cu 60000 0 0)
        )
        (attach off)
      )
    )
    (network_out 
      (net NA
        (wire
          (path F.Cu 2500
            1800000 1800000
            1800000 796733
            200000 796733
            200000 200000
          )
        )
      )
      (net NB
        (wire
          (path B.Cu 2500
            200000 1800000
            200000 796733
            1800000 796733
            1800000 200000
          )
        )
      )
    )
  )
)
================================================================================
#163 — the seven-of-eight doors, as the port's own pinning test prints it
================================================================================
crates/fr-router/tests/sorted_neighbours_regimes.rs:679
  an_obstacle_room_with_no_neighbours_skips_the_last_side_of_its_octagon

  room shape  = Oct[-1340,-240,-260,1440,-2464,-336,-1264,864]
  doors n=8            (1 dim-2 door to obs5/1, plus 7 dim-1 edge doors)
  incompleteRooms n=7  (sides 0..6; side 7 (-1340,76)->(-1024,-240) is skipped)

  the four incomplete-room shapes used to cross-check the octagon-normalisation model:
    [0] side 0  Oct[-10000,-10000,10000,-240,-9760,20000,-20000,9760]
    [5] side 5  Oct[-10000,-7536,7536,10000,-20000,-2464,-17536,17536]
    [6] side 6  Oct[-10000,-10000,-1340,10000,-20000,8660,-20000,8660]
    [2] side 2  Oct[-260,-10000,10000,10000,-10260,20000,-10260,20000]

================================================================================
#156 / #167 / #158 — the id arithmetic, from the port's sources
================================================================================
crates/fr-router/src/autoroute/expansion/obstacle_room.rs:97-100
  pub fn id(item: ItemId, index_in_item: usize) -> i32 {
      (item.0 as i32).wrapping_shl(10) | (index_in_item as i32)
  }
crates/fr-router/src/autoroute/expansion/door.rs:156-162
  pub fn id(first_id: i32, second_id: i32) -> i32 {
      first_id.min(second_id).wrapping_mul(31).wrapping_add(first_id.max(second_id))
  }
crates/fr-router/src/autoroute/drill/page.rs:445-455  — hash includes net_number, which get_drills overwrites
crates/fr-router/src/autoroute/expansion/incomplete_room.rs:56-91 — 31*shape.get_id()+layer; panics on the whole-plane room

Existing pinning tests that INVERT under the fix:
  incomplete_room.rs:154  the_id_is_the_shape_hash_times_31_plus_the_layer
  incomplete_room.rs:167  assert_ne!(room.get_id(), before)   -> becomes assert_eq!
  incomplete_room.rs:175  the whole-plane room's get_id() panics

================================================================================
#170 — the port already documents the NaN fall-through as non-total
================================================================================
crates/fr-router/src/autoroute/maze/list_element.rs:145-160
  'The comparator is also not a total order even before the resolver: quirk #170's NaN
   fall-through means a<b, b<a and a==b can all be false-ish ... Declaring [Ord] would be
   a lie the compiler cannot catch.'
  => there is deliberately NO impl Ord for MazeListElement today. The fix restores one.
