# Plan 8 pre-plan rulings (controller, 2026-08-30) — answers to survey-plan8.md §7

- **AO (Q1) MCP contract:** spec §13's four tools verbatim, flat arguments; Java's OpenAPI-derived registry `// not ported:`; keep `BoardFilePayload`'s field names (`job_id`, `data`, `size`, `crc32`, `format`, `statistics`, `filename`, `path`) where the outputs overlap; `p8t6` is a documented-delta driver (each delta listed with the Java behaviour beside it), not parity.
- **AP (Q2) Cancellation:** option (a) — an external `Arc<AtomicBool>` `CancelToken` that the routing thread copies into `RouterStop` at the six existing poll sites. Plan 7's interface untouched. If a poll site turns out to be missed, add a poll, never a lock.
- **AQ (Q3) Dead legacy flags stay dead** (`-oit`, `-us`, `-is`, `-hr`, `-inc`, `-drc`'s router switch, `-mt`/`--threads`) — quirks #131/#143 reproduced; the same knobs are exposed only via the native `--set section.field=value` path where no jar disagrees. Recorded as a product decision in the hand-off.
- **AR (Q4) CLI compat:** jar flag syntax bug-for-bug on the legacy path (`startsWith` matching, silent missing-value no-op, warn-and-continue on unknown flags, exit only 0/1 — map every legacy failure to 1). Exit codes 2/3 reserved for the native subcommand form, recorded as a port-only extension. `-di` stays unsupported; no `-v` (Java's is `-ll`). `p8t5` measures the legacy surface.
- **AS (Q5) Rostered not ported:** `BoardComparator` + `--compare-boards`, `BoardScoreBreakdown`, `ScoringWeightComparison` (dead reachability, known-divergent formula = quirk-X label). Stated in the hand-off.
- **AT (Q6) Version strings:** one `fr_core::PARITY_VERSION` (= the jar value each format surface was pinned against) for every FILE-FORMAT field (SES/DSN `(host …)`, DRC `freerouting_version`, manifest `app_version`); `CARGO_PKG_VERSION` only in MCP `serverInfo`; keep the newer MCP `protocolVersion` (the port is a new server).
- **AU (Q7) No HTTP API.** `api/**` rostered in full (8 425 lines; `JobControllerV1` provably dead). `ApiSettings`' priority-70 tier STAYS — the MCP `route_board { settings? }` argument composes exactly that tier per `resolve.rs:184`.
- **AV (Q8) Jars and gates:** end-to-end `p8t*` drivers run against the HEAD jar (it is what routes/DRCs; Plan 3's 2.3.0 pins already cover the DSN/SES format surfaces and stay untouched). `p8t1`'s gate is the SES BYTE-diff plus exit code plus normalised-log comparison; any divergence is XDIFF-with-root-cause, never tolerance.
- **Risk 2 (`HeadlessBoardManager` overrides, ~370 lines):** Plan 8 owns porting `applyCopperToEdgeClearanceOverride`/`applyHoleClearanceOverride`/`assignHoleKeepoutClearanceClass` as an early task (they run on every real DSN load), UNLESS Plan 7 Task 16 finds they fire on the batch corpus — then Plan 7 gains an insert task and Plan 8 inherits only the audit. `management/` gets an audit map either way.
- **General:** quirk ids assigned at write time from the register's last row; task size ≤ ~600 lines; new-code tasks (MCP) pinned by contract tests + the documented-delta driver; no new deps without a ruling (the MCP schema needs may NOT add `schemars` — hand-write the four tool schemas as JSON literals; revisit only if that proves error-prone).
